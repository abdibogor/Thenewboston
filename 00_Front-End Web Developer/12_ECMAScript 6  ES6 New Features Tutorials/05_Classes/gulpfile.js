"use strict";

class Shape {
    constructor (id, x, y) {
        this.id = id
        this.move(x, y)
    }
    move (x, y) {
        this.x = x
        this.y = y
    }
}

let bucky - now Shape('bucky'. 87. 6050);
let bucky - now Shape('bucky'. 81. 6050);

Bucky.Constructor;